# ADR 0003: Large File Refactor Targets

Date: 2026-04-20

## Status

Accepted

## Context

Files over 400 LOC are allowed only when the boundary is explicit and the next
refactor target is known. These files slow review and increase regression risk,
so they are tracked before broad feature work resumes.

## Decision

`npm run quality:files` fails when a source file over 400 LOC is not documented
here. Current oversized files:

| File                                                              |  LOC | Current responsibility                                                                                                                                | First safe refactor                                                                                                                               |
| ----------------------------------------------------------------- | ---: | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/domain/gardens/validation.ts`                                | 1418 | Runtime parsing and sanitization for every garden aggregate record, including sun/shade source metadata and microclimate notes.                       | Split by record family: plot/structures/plantings, tasks, journal/harvests, notifications/users.                                                  |
| `src/domain/gardens/sampleGarden.ts`                        | 1055 | Deterministic demo aggregate and profile builder.                                                                                                     | Move demo structures, plantings, operations, log records, and sun layers into named fixture builders.                                             |
| `src/features/garden/useGarden.ts`                                | 1033 | Plan editor orchestration, save state, history, placement actions, weather refresh, and sun painting.                                                 | Extract pure editor reducers/actions and keep the hook as repository and UI coordination only.                                                    |
| `src/features/tasks/taskEngine.ts`                                | 1026 | Generated tasks, task state transitions, succession suggestions, and date helpers.                                                                    | Split generated task builders from task mutation actions and succession planning.                                                                 |
| `src/features/tasks/taskEngine.test.ts`                           |  405 | Task-engine coverage for generated water/weather/crop work, state transitions, and succession task behavior.                                          | Split watering/task lifecycle cases from succession recommendation coverage once the watering schedule model stops moving.                        |
| `scripts/build-home-garden-catalog.mjs`                           |  991 | Offline plant catalog generator, including seed additions, defaults, generated variety-group records, and provenance output.                          | Move base additions and variety banks into JSON data files while keeping the generator as orchestration only.                                     |
| `functions/index.js`                                              |  853 | Cloud Functions entrypoints plus notification dispatch implementations.                                                                               | Move FCM and notification-log dispatch into small modules.                                                                                        |
| `functions/wateringLogic.js`                                      |  407 | Backend weather snapshot and watering schedule generation plus legacy-preserving merge behavior.                                                      | Split target discovery, schedule-entry building, rationale copy, and merge semantics into focused helpers.                                        |
| `functions/weatherProviderUtils.js`                               |  448 | Shared Cloud Functions weather cache, NWS/Tomorrow parsing, precipitation conversion, and daily forecast rollup helpers.                              | Split cache/request utilities from forecast/grid normalization helpers after selected-day weather behavior stabilizes.                            |
| `scripts/seed-dev.mjs`                                            |  795 | Firebase Auth lookup plus demo seed document generation and writes.                                                                                   | Reuse the demo builder more directly and isolate Firebase write planning from CLI parsing.                                                        |
| `e2e/app-smoke.spec.ts`                                           |  435 | Broad route smoke coverage for auth, Plan editing, sample-garden flow, publish/revert, and offline Feed behavior.                                     | Split auth/session smoke coverage from sample-garden and workspace-route walkthroughs so each spec owns one app story.                            |
| `e2e/today-tasks.spec.ts`                                         |  434 | Today route coverage for generated tasks, watering flows, real-weather refresh fixtures, selected-day forecasts, and harvest sheets.                  | Split weather/watering refresh coverage from harvest/task field-action coverage once Today’s weather panel settles.                               |
| `src/domain/gardens/models.ts`                                    |  882 | Canonical garden, user, task, weather, notification, sun/shade, and helper model definitions.                                                         | Split model types by domain family while keeping `GardenRepository` exports stable.                                                               |
| `src/app/router.test.tsx`                                         |  540 | Route-level integration coverage for auth routing, Today weather refresh wiring, and service overrides.                                               | Split Today refresh routing coverage from general auth/app-shell route coverage once weather service wiring stops moving.                         |
| `src/features/plan/PlanPage.tsx`                                  | 1153 | Plan route orchestration across draft/publish state, mode state, modals, warnings, operations, and layout.                                            | Move draft/publish modal state and warning state into route-owned hooks, then keep JSX composition only.                                          |
| `src/features/plan/autoLayoutEngine.test.ts`                      |  441 | Auto-layout solver coverage for deterministic proposals, constraints, support representation, trellis creation, and variant ranking.                  | Split support/trellis proposal cases from whole-plot candidate ranking once the Prompt 27 support model settles.                                  |
| `src/features/garden/planHealthRules.ts`                          |  635 | Plan-health diagnostics and material add-on rules for support, access, irrigation, rotation, capacity, mulch, and seasonal protection.                | Split material add-on builders from health issue builders once the rule copy and severity model stabilize.                                        |
| `src/features/garden/wateringEngine.ts`                           |  574 | Client-side weather snapshot and water recommendation engine.                                                                                         | Extract target discovery, deficit math, rationale copy, and data-quality scoring.                                                                 |
| `src/features/garden/wateringScheduleRefresh.test.ts`             |  423 | Watering refresh coverage for snapshot rebuilds, live weather refresh options, task sync, and save-safe merge behavior.                               | Split live provider refresh cases from persisted-snapshot rebuild cases after weather refresh contracts stabilize.                                |
| `src/features/garden/wateringOutlook.ts`                          |  788 | Two-week watering outlook projection, grouped run aggregation, forecast-day rollups, and manual-watering history carry-forward for Today scheduling.  | Split target discovery/current-balance math from forecast projection and grouped-run aggregation once the multi-day watering horizon settles.     |
| `src/features/plan/planInteractionGeometry.ts`                    |  559 | Feet-based precision interaction math for snap targets, drag offsets, resize handles, marquee hit tests, and group transforms.                        | Split snap target generation, resize math, and selection hit testing into separate pure modules.                                                  |
| `src/features/plan/hooks/usePlanPointerInteractions.ts`           |  543 | Pointer orchestration for drag, resize, snap guides, touch thresholds, group movement, and marquee selection.                                         | Extract drag, resize, and marquee state machines into focused hooks after behavior stabilizes.                                                    |
| `src/features/plan/autoLayoutScoring.ts`                          |  576 | Candidate quality scoring for access routes, spacing, water grouping, structure compatibility, and gardener-facing scoring breakdowns.                | Split access-route evaluation from spacing/compatibility scoring, keeping shared score types and breakdown assembly at top-level.                 |
| `src/features/log/LogPage.module.css`                             |  511 | Feed route layout, forms, cards, tabs, and analytics presentation styles.                                                                             | Split form, workspace, card, and analytics style modules with no visual redesign.                                                                 |
| `src/features/log/useLogController.ts`                            |  423 | Feed route data loading, filter state, journal/harvest submission, photo upload, and save-status orchestration.                                       | Split garden loading/filter derivation from journal/harvest mutation handlers while keeping the hook as the route boundary.                       |
| `src/infrastructure/firebase/gardens/firebaseGardenRepository.ts` |  504 | Firebase implementation for legacy garden migration, shared workspace reads, per-user drafts, revision writes, and offline draft queueing.            | Split workspace/revision document mapping from Firebase transaction orchestration, preserving `GardenRepository`.                                 |
| `src/domain/gardens/validation.test.ts`                           |  415 | Broad parser and compatibility coverage for garden aggregate records, user profiles, notifications, tasks, harvests, and journals.                    | Split parser tests by record family beside future validation module splits.                                                                       |
| `src/features/plan/components/PlanInspectorPanels.tsx`            |  503 | Route inspector tab panels for planting and structure details, care settings, sun fit, microclimate notes, schedule, warnings, and history.           | Split planting panels and structure panels into separate files while keeping the inspector shell unchanged.                                       |
| `src/features/garden/gardenWarningChecks.ts`                      |  554 | Plan review checks for spacing grouping, structures, paths, bed fit, sun uncertainty, trellis support, and rotation.                                  | Split path/structure access checks from crop fit/support checks after the warning copy stabilizes.                                                |
| `src/features/garden/reviewSuggestions.test.ts`                   |  409 | Review suggestion coverage for support, trellis, access, movement, and batch-accept plan mutations.                                                   | Split support/trellis suggestion cases from movement/access suggestion cases after review actions stabilize.                                      |
| `src/infrastructure/weather/nwsWeatherProvider.ts`                |  457 | NWS point lookup, forecast, alerts, precipitation, and condition mapping.                                                                             | Split NWS request helpers from weather normalization.                                                                                             |
| `src/infrastructure/weather/tomorrowIoWeatherProvider.ts`         |  407 | Optional Tomorrow.io adapter with NWS fallback, hourly timeline parsing, daily forecast rollup, and agriculture metrics.                              | Split Tomorrow.io timeline normalization from fallback/provider orchestration if the optional provider gains more product-specific behavior.      |
| `src/features/garden/GardenEditorScreen.test.tsx`                 |  644 | Plan route behavior coverage across setup, placement, inspection, warnings, operations, precision selection, and undo/redo.                           | Split setup, placement, operations, and precision-interaction tests into route-specific test files.                                               |
| `src/features/garden/gardenPlanning.test.ts`                      |  469 | Feet-based planning warning coverage for footprints, grouped spacing, access, sun uncertainty, succession timing, and rotation.                       | Split warning taxonomy and succession-timing cases into a dedicated warning-engine test file.                                                     |
| `src/features/plan/components/PlanCanvasItems.module.css`         |  475 | Canvas item styling for sun cells, microclimate markers, plant footprints, structure types, selection, warnings, locking, and handles.                | Split sun-overlay styles from plant/structure and resize-handle styles once the interaction visuals stabilize.                                    |
| `src/features/plan/components/PlanCanvas.tsx`                     |  455 | Canvas viewport orchestration for zoom/pan controls, layer toggles, transient pointer wiring, and scene composition.                                  | Split viewport-control state and pointer bridge callbacks from the scene container once interaction semantics stop moving.                        |
| `src/features/plan/components/PlanCanvasScene.tsx`                |  402 | Plan canvas scene composition for overlays, snap guides, transient drag/resize previews, structures, and grouped plant renderers.                     | Split transient interaction overlays from structure/plant scene rendering once the zero-lag interaction model settles.                            |
| `src/features/plan/components/PlanPlantGroup.tsx`                 |  472 | Grouped plant footprint renderer, label/wrench interactions, support badges, warnings, and crop-focused visual states.                                | Extract support badge derivation and plant signal rendering after the support states stop changing.                                               |
| `src/features/plan/components/PlantEditorSheet.module.css`        |  415 | Plant editor sheet styling for the pinned drawer, care/event logger, arrangement controls, support context, warnings, and responsive editor layout.   | Split the event logger and summary-strip styles from the generic sheet/layout rules once the planting-event UI settles.                           |
| `src/domain/gardens/gardenWorkspace.ts`                           |  433 | Shared garden workspace model helpers for published revisions, per-user drafts, changeset summaries, and serialized suggestion decisions.             | Split parsing/normalization helpers from pure changeset comparison helpers.                                                                       |
| `src/features/plan/components/PlanModeDrawer.module.css`          |  623 | Mode drawer controls, mobile mode strip, sun/structure controls, Plan health panel, and Review proposal-inbox card styling.                           | Split sun-control, Plan health, and review-inbox styles from generic mode drawer controls once diagnostics stabilize.                             |
| `src/features/plan/components/PlanReviewPanel.tsx`                |  576 | Problem inbox cards, structured evidence, resolution actions, ignored/applied states, and reason tooltip copy.                                        | Extract problem-card and resolution-option card components once the problem-resolution model stabilizes.                                          |
| `src/features/plan/components/PlanOptimizeCandidates.module.css`  |  447 | Variant comparison, checked-layout card rail, downstream validation, tradeoff badges, and mobile optimizer layouts.                                   | Split variant rail styles from selected-variant detail styles after the optimizer comparison flow settles.                                        |
| `src/features/garden/AddPlantModal.tsx`                           |  420 | Quantity-first crop picker modal, arrangement controls, live placement preview, filters, comparison, and modal accessibility wiring.                  | Move crop search state and arrangement preview wiring into small hooks after Add Plant and Choose Plants converge.                                |
| `src/features/garden/cropPickerHelpers.ts`                        |  487 | Gardener-facing crop picker filtering, sorting, location/timing labels, result ranking, and catalog presentation helpers.                             | Split ranking/suitability sorting from copy-formatting helpers once the Add Plant information architecture settles.                               |
| `src/domain/crops/plantLocationMatch.ts`                          |  482 | Garden-location context, frost-window timing guidance, sun and hardiness matching, and explainable crop fit scoring.                                  | Separate context builders, timing-window evaluation, and explanation copy into focused pure modules after location-fit rules stop moving.         |
| `src/features/garden/CropPickerPanels.module.css`                 |  401 | Add Plant crop result rows, detail card, compare panel, arrangement editor, filters, and advanced disclosure styling.                                 | Split result-list/detail-card styles from arrangement/filter styles after the Add Plant modal layout stabilizes.                                  |
| `src/features/plan/components/ChoosePlantsModal.tsx`              |  410 | Choose Plants modal shell, season-board state, compare state, footer actions, tabs, and dialog focus management.                                      | Extract selection state and mobile-tab shell into dedicated helpers after the modal contract stabilizes.                                          |
| `src/features/shared/design/DesignPrimitives.tsx`                 |  446 | Shared semantic UI primitives for actions, status, filters, chips, empty states, and callouts across Plan, Today, Feed, Review, and shell.            | Split passive display primitives from interactive action/filter primitives after Prompt 15 migrations stabilize.                                  |
| `src/features/shared/design/DesignPrimitives.module.css`          |  443 | Shared semantic primitive styling for action intent, status tone, info chips, segmented controls, empty states, callouts, and cards.                  | Split action/filter styles from passive display styles after the semantic system has been reused across all routes.                               |
| `src/features/settings/SettingsPage.module.css`                   |  412 | Settings route layout and section styling for profile, alerts, device status, sample-garden controls, and responsive settings panels.                 | Split sample-garden disclosure and alert-history styles from the base settings layout once the simplified Settings UI settles.                    |
| `src/features/today/TodayPage.tsx`                                |  410 | Today route orchestration for loading the garden, field quick actions, task mutations, harvest follow-up, and route composition.                      | Extract Today quick-action submit handlers into a route-owned hook after Prompt 30 settles the compose pattern.                                   |
| `src/features/today/TodayPage.module.css`                         |  414 | Today route layout, task cards, field panels, action rows, summaries, and responsive task interactions.                                               | Split task-card and field-panel styles from route layout after Today action semantics finish settling.                                            |
| `src/features/today/todayActions.ts`                              |  629 | Today-side field mutations for watering logs, snooze/skip/adjust flows, issue notes, harvest logging, lifecycle updates, and task refreshes.          | Split watering actions from journal/harvest/lifecycle mutations after the first-class watering work surface settles.                              |
| `src/features/today/todayActions.test.ts`                         |  415 | Today action coverage for watering completion, issue logging, harvest flow, lifecycle updates, and generated task synchronization.                    | Split watering/issue actions from harvest/lifecycle action coverage once the watering schedule and field-action model stop moving.                |
| `src/features/today/components/TodayFieldPanels.module.css`       |  438 | Today field-panel styling for watering cards, weather facts, harvest controls, compact checks, and responsive action layouts.                         | Split watering/weather panel styles from harvest/check card styles after Today’s field-work interaction model stops moving.                       |
| `e2e/redesign-regression.spec.ts`                                 |  432 | Cross-route regression coverage for chooser/layout handoffs, Add Plant layout stability, reduced-motion behavior, and simplified production surfaces. | Split route hierarchy and modal-layout assertions from layout-generation and reduced-motion flows so each spec owns one settled product contract. |

## Consequences

- These files can remain during the foundation pass because they represent
  working functionality.
- Any new file crossing 400 LOC needs either a refactor before merge or an
  added row in this ADR.
- Refactors should preserve the existing seams: `AuthService`,
  `GardenRepository`, `UserProfileRepository`, `WeatherProvider`,
  `NotificationService`, and `MediaStorageService`.
